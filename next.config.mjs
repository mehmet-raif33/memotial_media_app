/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com'], // Dış görüntülerin yüklenmesine izin verilen alan adlarını ekleyin
    },
};

export default nextConfig;
